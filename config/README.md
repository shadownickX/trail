# Configuration

Document public configuration shapes and safe defaults here. Runtime provider credentials belong only in the Site secret store and are never committed.

Keep chain IDs, supported windows, classifier thresholds, and feature flags explicit. Every setting should state its source, default, and effect on coverage.