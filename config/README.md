# Configuration

This directory will contain checked-in configuration schemas and safe defaults for chain access, indexing windows, and event-classification thresholds.

Secrets never belong here. Provider credentials must be injected through environment variables and documented with placeholder names only.
