# Data conventions

Local fixtures and reproducible public-chain captures belong here. Every dataset must record its chain, block range, capture time, source, and known gaps.

Raw wallet history should not be committed as an unexplained dump. Prefer small, reviewable fixtures that demonstrate one edge case.
