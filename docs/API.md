# TRAIL API

The live scanner accepts `POST /api/scan` with a full Robinhood Chain token contract and a supported lookback window. Responses include live observations, destination groups, and explicit coverage. Runtime credentials never reach the browser.