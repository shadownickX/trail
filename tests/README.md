# Testing

Tests should cover event classification, temporal ordering, deduplication, relationship provenance, incomplete-provider responses, and deterministic result grouping.

Public transactions used as fixtures must include block numbers and expected observations.
