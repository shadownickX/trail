# Scripts

Repeatable maintenance commands belong here: fixture capture, schema validation, deterministic asset rendering, and benchmark runners.

Every script should be non-interactive by default, document its inputs, and avoid signing or submitting transactions.
